GO
USE [SEDPLAN];
GO

ALTER FUNCTION DBO.FUN_PROCESS_EXISTS(@Process_ID BIGINT)
RETURNS BIT
AS
BEGIN

	DECLARE @ISEXISTS BIT;

	IF EXISTS
	(
		SELECT SP.Process_Name
		FROM SA_Process SP
		WHERE SP.Process_ID=@Process_ID
	)
		SET @ISEXISTS=1;
	ELSE
		SET @ISEXISTS=0;

	RETURN @ISEXISTS;

END